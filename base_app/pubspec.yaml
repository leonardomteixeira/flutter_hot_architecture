name: clean_architecture_proposal
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  micro_core:
    path: ../micro_core
  micro_app_search:
    path: ../micro_app_search
  micro_app_about:
    path: ../micro_app_about
  micro_commons_deps:
    path: ../micro_commons_deps

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
